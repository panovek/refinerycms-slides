<%= render 'refinery/admin/search_header', :url => refinery.slides_admin_slides_path %>
<div class='pagination_container'>
  <% if @slides.any? %>
    <%= render 'slides' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
